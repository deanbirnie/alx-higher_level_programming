#!/usr/bin/python3
"""Check to see if an object is an instance of the specified class"""


def is_same_class(obj, a_class):
    """Check type against specified class"""
    return type(obj) is a_class
